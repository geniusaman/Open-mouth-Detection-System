### Usage:
1. _to install requirnments modules and library run below code_
```
!pip install -r requirnment
```
2. _After installing requirnments python modules and library next step is to run main.py file_
```
!python main.py
```
> make sure to change .yml file path in main.py path
